# Copy to .env and fill values for local development.

# Database connection string. Matches the docker-compose Postgres service.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/billpay?schema=public"

# HTTP port the API listens on. Frontend dev server takes 3000.
PORT=3001
